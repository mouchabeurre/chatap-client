import { FriendfilterPipe } from './friendfilter.pipe';

describe('FriendfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendfilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { ScrollDownStreamDirective } from './scroll-down-stream.directive';

describe('ScrollDownStreamDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDownStreamDirective();
    expect(directive).toBeTruthy();
  });
});

import { ShareChatVhDirective } from './share-chat-vh.directive';

describe('ShareChatVhDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareChatVhDirective();
    expect(directive).toBeTruthy();
  });
});
